# @title #**Project background** 
#This will be a short video introducing the content creator(s) and motivating the research direction of the template.
#The Tech team will add code to format and display the video

#Title: Sea Level Rise

# Imports

#Import only the libraries/objects that are necessary for more than one dataset. 
#Dataset-specific imports should be in the respective notebook section.

#If any external library has to be installed, !pip install library --quiet
#follow this order: numpy>matplotlib. 
#import widgets in hidden Figure settings cell

import random
import numpy as np
import matplotlib.pyplot as plt

!pip install netCDF4 --quiet
!pip install "xarray[complete]"  --quiet
!pip install aiohttp --quiet
!pip install cftime --quiet

import xarray as xr

from google.colab import drive
drive.mount('/content/drive')

# example plot. Plots map of sea surface height at one time-stamp.

plt.pcolormesh(ds['zos'][1500,:,:]); plt.colorbar()
plt.grid(True)

# Data source-specific imports

# Data source:   https://pangeo-forge.org/dashboard/feedstock/7
## CMIP6.CMIP.CCCma.CanESM5.historical.r1i1p1f1.Omon.zos.gn.v20190429.zarr

import sys
import xarray as xr
sys.path.append('/content/drive/Shareddrives/Academy/Courses/Climate/Climatematch/06-Projects/01-Resources/Data exploration notebooks/Sea level rise (Aakash)')  
ds=xr.open_dataset('/content/drive/Shareddrives/Academy/Courses/Climate/Climatematch/06-Projects/01-Resources/Data exploration notebooks/Sea level rise (Aakash)/SEA_SURFACE_HEIGHT_mon_mean_1992-01-2017-12_ECCO_V4r4_latlon_0p50deg.nc')

ds

ds['SSH'][:,200,134].plot()

ds['SSH'][100,:,:].plot.pcolormesh()

# Students can download any tidal gauge data of their choice from this website: 
# https://uhslc.soest.hawaii.edu/data/

from IPython.core.display import display_json
# Data source-specific functions

#example code after downloading tidal gauge data:
ds=xr.open_dataset('d007.nc'). # this is just an example, tidal gauge NetCDF file needs to be downloaded in order to load this.
ds

